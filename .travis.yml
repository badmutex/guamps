language: c
compiler: clang

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install gromacs

install:
  - mkdir build
  - cd build
  - cmake ..
  - make
  - sudo make install
  - cd ..

script: ./run_tests
